@model RecoveryCodesModel
@{
    ViewData["Title"] = "Коды восстановления";
}

<partial name="_AlertPartial" />
<h4>@ViewData["Title"]</h4>
<div class="alert alert-warning" role="alert">
    <p>
        <strong>Сохраните коды в безопасном месте!</strong>
    </p>
    <p>Если у вас не будет доступа к устройству для двухфакторной авторизации, вы сможете получить доступ к аккаунту с помощью кода восстановления</p>
</div>
<div class="row">
    <div class="col-md-12">
        @for (int row = 0; row < Model.RecoveryCodes.Length; row += 2)
        {
            <code class="recovery-code">@Model.RecoveryCodes[row]</code><text>&nbsp;</text><code class="recovery-code">@Model.RecoveryCodes[row + 1]</code><br />
        }
    </div>
</div>